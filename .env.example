# =============================================================================
# CS Risk Agent - 環境変数テンプレート
# =============================================================================
# このファイルを .env にコピーして、実際の値を設定してください
# cp .env.example .env
# =============================================================================

# --- Application ---
APP_ENV=development
APP_DEBUG=true
APP_SECRET_KEY=change-me-to-a-random-secret-key
APP_HOST=0.0.0.0
APP_PORT=8005

# --- CORS ---
# カンマ区切りでフロントエンドのオリジンを指定
CORS_ORIGINS=["http://localhost:3005","http://localhost:3000"]

# --- Database ---
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/cs_risk_agent
DATABASE_SYNC_URL=postgresql://postgres:postgres@localhost:5432/cs_risk_agent

# --- Redis ---
REDIS_URL=redis://localhost:6379/0

# --- Azure AI Foundry ---
AZURE_AI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_AI_API_KEY=
AZURE_AI_API_VERSION=2024-12-01-preview
AZURE_SOTA_DEPLOYMENT=gpt-4o
AZURE_COST_EFFECTIVE_DEPLOYMENT=gpt-4o-mini

# --- AWS Bedrock ---
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_BEDROCK_SOTA_MODEL=anthropic.claude-3-5-sonnet-20241022-v2:0
AWS_BEDROCK_COST_EFFECTIVE_MODEL=anthropic.claude-3-haiku-20240307-v1:0

# --- GCP Vertex AI ---
GCP_PROJECT_ID=
GCP_LOCATION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=
GCP_SOTA_MODEL=gemini-1.5-pro
GCP_COST_EFFECTIVE_MODEL=gemini-1.5-flash

# --- Local LLM (Ollama) ---
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_SOTA_MODEL=llama3.1:70b
OLLAMA_COST_EFFECTIVE_MODEL=llama3.1:8b

# --- Local LLM (vLLM) ---
VLLM_BASE_URL=http://localhost:8080
VLLM_MODEL=meta-llama/Llama-3.1-8B-Instruct

# --- AI Orchestration ---
# AI_MODE: cloud / local / hybrid
#   cloud  - クラウドプロバイダー (Azure/AWS/GCP) を使用
#   local  - ローカル Ollama のみ使用 (機密データ向け、コスト0)
#   hybrid - データ分類に基づきクラウド/ローカルを自動選択
#
# ローカル開発で Ollama を使う場合:
#   AI_MODE=local
#   AI_DEFAULT_PROVIDER=ollama
#   AI_FALLBACK_CHAIN=ollama
#   + ollama serve && ollama pull llama3.1:8b
AI_DEFAULT_PROVIDER=azure
AI_FALLBACK_CHAIN=azure,aws,gcp,ollama
AI_MODE=cloud
AI_MONTHLY_BUDGET_USD=500.0
AI_BUDGET_ALERT_THRESHOLD=0.8
AI_CIRCUIT_BREAKER_THRESHOLD=0.95

# --- EDINET API ---
EDINET_API_KEY=
EDINET_BASE_URL=https://api.edinet-fsa.go.jp/api/v2

# --- Observability ---
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
OTEL_SERVICE_NAME=cs-risk-agent
LOG_LEVEL=INFO
LOG_FORMAT=json

# --- JWT Auth ---
JWT_SECRET_KEY=change-me-to-a-random-jwt-secret
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=60
